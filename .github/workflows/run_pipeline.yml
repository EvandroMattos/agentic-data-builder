name: Run Pipeline

on:
  pull_request:
    types: [closed]

jobs:
  run-databricks:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Run script (mock or real)
        run: |
          echo "Pipeline would run in Databricks here"